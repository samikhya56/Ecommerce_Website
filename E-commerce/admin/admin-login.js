document.getElementById('loginForm').addEventListener('submit', function(event) {
    event.preventDefault();
    // Logic to validate login credentials (to be implemented)
    window.location.href = 'admin-dashboard.html'; // Redirect to dashboard on successful login
});
